import type { Job } from 'pg-boss'

/** Parabeniza os aniversariantes do dia (CT-035). Nunca duas vezes no mesmo ano. */
export async function parabenizarAniversariantes(_jobs: Job[]) {
  console.log('TODO CT-035')
}
